<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.useopencvwithcmakeandkotlin">

    <!-- Android 13 (API 33) 이상을 위한 권한 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Android 12 이하를 위한 권한 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28"/>

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.UseOpenCVWithCMakeAndKotlin">
        
        <activity
            android:name=".StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".MainActivity"
            android:exported="true">

        </activity>

        <activity
            android:name=".ROIActivity"
            android:exported="true">
        </activity>

        <activity
            android:name=".HSVActivity"
            android:exported="false" />

        <activity android:name=".MarkerCenterActivity"
            android:exported="false" />

        <activity android:name=".DisplacementActivity"
            android:exported="false"  />

        <activity android:name=".VideoSizeActivity"
            android:exported="false"/>

        <activity
            android:name=".MarkerColorActivity"
            android:exported="false" />

        <activity
            android:name=".FaultDiagnosisActivity"
            android:exported="false" />

        <activity
            android:name=".TestActivity"
            android:exported="false" />

    </application>

</manifest>